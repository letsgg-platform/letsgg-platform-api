# letsgg-platform-api
Backend API for let's GG Platform

## Requirements
- **JDK >= 11**
- **Maven >= 3.6.3**
- **MongoDB >= 4.4**

## Build


```bash
mvn clean package -DskipTests
```

## Test
```bash
mvn clean verify
```
